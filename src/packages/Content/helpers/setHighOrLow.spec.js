import sinon from 'sinon';
import setHighOrLow from './setHighOrLow';

var stub = sinon.stub(setHighOrLow, "randomizer");

describe('The setHighOrLowFunction', ()=> {
    it('should only be called with 0 and 1', () => {
            
    }
    )
})
